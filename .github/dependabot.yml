# Grouped Dependabot updates: one PR per ecosystem (Ruby / JavaScript / GitHub Actions)
# instead of one PR per dependency. Tracks npm so the shakapacker gem and npm
# package move together. 3-day cooldown so a freshly published version can't be
# yanked after the PR opens. Patterned on shakacode/react_on_rails.
version: 2
updates:
  - package-ecosystem: "bundler"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    cooldown:
      default-days: 3
    groups:
      react-on-rails-product-family:
        patterns:
          - "react_on_rails*"
      ruby:
        patterns:
          - "*"

  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    cooldown:
      default-days: 3
    ignore:
      # Shakapacker 10 supports webpack 5; keep Dependabot from proposing an
      # incompatible webpack 6 migration before the integration supports it.
      - dependency-name: "webpack"
        versions: [">= 6.0.0"]
    groups:
      react-on-rails-product-family:
        patterns:
          - "react-on-rails*"
      javascript:
        patterns:
          - "*"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    cooldown:
      default-days: 3
    groups:
      github-actions:
        patterns:
          - "*"
